// Declaring a variable in JavaScript using var keywords.
var num;

num = 10;

// Declaring and initialing
var num = 10;

// Variables are dynamically typed
var num = 10;
var firstName = "Shashank";
var isJavaScript = true;

var nothing = null;

var list = [1, 2, 3, 4, 5, 6, 7];
var list = [1, "hi", true];
var list = [1, "hi", true, [1, 2, 3]];

var month = {
  key: value,
  1: "jan",
  num: value,
};

//
var arr = [1, 2, 3, 4, 5];
var arr1 = [1, 2, "js", true, ["one", "two"], arr];

var obj = {
  1: "one",
  2: "two",
  list: ["2", true],
};

var obj2 = {
  one: 1,
  two: 2,
};

var res = [obj, obj2];
